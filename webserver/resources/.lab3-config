# These are comment lines and should be ignored by config parser (as well as blank lines)

# Listening port
port = 80

# Run as daemon?
as_daemon = false

# Webserver document root directory path
# Default: ../../www (config parser should get absolute path)
doc_root_dir = ../../www